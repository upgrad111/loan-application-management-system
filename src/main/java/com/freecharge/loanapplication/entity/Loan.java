package com.freecharge.loanapplication.entity;



/*
 * Add the following annotations to the Loan class
 * @Entity
 * @Table with loan_application_details as table name
 */

public class Loan {
	
	/*
	 * Define the following properties along witht the mapped column names
	 * ----------------------------------------
	 * Property                   column name
	 * id						  loan_id
	 * applicantName		      applicant_name
	 * applicantEmail             applicant_email
	 * applicantContactNo         applicant_contactno
	 * loanAmount                 loan_amount
	 * appliedDate                loan_applied_date   ( Use DateTimeFormt as ISO )
	 * status					  status
	 */
	
	
	
	/*
	 * Define no-arg constructor
	 */
	
	/*
	 * Define getter and setter method for each property
	 */
	
	/*
	 * Override toString() method will all the properties
	 */
	
	
}
